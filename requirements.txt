# Core dependencies — required for the synthetic benchmarks, the experiment
# runner, metrics, and control-theoretic analysis. Everything in the M8 core
# evidence path runs with only these.
numpy<2
pandas
matplotlib
scipy
networkx

# --- Optional extras ---------------------------------------------------------
# MARL benchmarks (PettingZoo MPE). On Python 3.9 the MPE environments live in
# the separate `mpe2` package:
#   pip install pettingzoo gymnasium mpe2
#
# Infrastructure benchmark (water networks). On Python 3.9 pin:
#   pip install "wntr==1.3.2" "setuptools<80"
#
# Milestone 9 physical-infrastructure benchmarks (electric power + gas networks):
#   pip install pandapower pandapipes        # keep numpy<2 pinned
#
# Milestone 9 learned coordinators (graph-attention GNN, reward-redistribution
# RECO). CPU is sufficient; GPU is used automatically if present:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# A fully pinned environment used for the released experiments is in
# requirements-lock.txt.
